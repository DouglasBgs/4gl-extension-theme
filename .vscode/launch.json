{
  "configurations": [
    {
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "name": "Launch Extension",
      "outFiles": ["${workspaceFolder}/dist/js/**/*.js"],
      "request": "launch",
      "type": "extensionHost"
    }
  ]
}
